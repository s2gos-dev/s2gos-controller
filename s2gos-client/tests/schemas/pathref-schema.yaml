type: object
properties:

  user_models:
    type: object
    title: Extra models
    properties:
      value:
        title: User data URL
        type: string
      cid:
        title: User data CID
        type: string

  user_regions:
    type: object
    title: Extra regions
    properties:
      value:
        title: User data URL
        type: string
      cid:
        title: User data CID
        type: string
